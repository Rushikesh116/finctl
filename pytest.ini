[pytest]
testpaths = tests
# Repo root on sys.path so tests can `from core.money import ...` without a conftest hack.
pythonpath = .
python_files = test_*.py
addopts = -ra -q --strict-markers --strict-config
